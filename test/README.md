# Test Suite for SkipperWatch

This directory contains unit tests for the SkipperWatch Garmin Connect IQ application.

## Running Tests

```bash
run_tests.sh
```

## Running Simulator

```bash
build.sh && start_simulator.sh
```